import { MilesConvertPipe } from './miles-convert.pipe';

describe('MilesConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new MilesConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
